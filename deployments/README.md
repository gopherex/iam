# deployments/

Everything needed to deploy IAM to real environments: container image
definitions, Kubernetes / Helm manifests, Terraform, and release config.

This is **production** deployment material. The local development environment
lives in the root [`docker-compose.yml`](../docker-compose.yml) instead.

Contents are added once the runtime stack and target platform are fixed.
