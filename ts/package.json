{
  "name": "@gopherex/iam-ts",
  "private": true,
  "description": "TypeScript workspace for the IAM SDK.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate": "yarn workspace @gopherex/iam-sdk generate",
    "build": "yarn workspaces run build"
  }
}
