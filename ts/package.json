{
  "name": "@gopherex/iam-ts",
  "private": true,
  "description": "TypeScript workspace for the IAM SDK.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate": "echo \"TODO: generate packages/sdk from ../openapi/openapi.yaml\" && exit 0",
    "build": "yarn workspaces foreach -A run build"
  }
}
